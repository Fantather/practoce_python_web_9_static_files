document.addEventListener('DOMContentLoaded', ()=>{
    const myButtonEl = document.getElementById('myButton');
    if(myButtonEl) myButton.addEventListener('click', ()=>{
        alert('Click button')
    })
})